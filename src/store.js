import { reactive } from "vue";

export const store = reactive({});

/*
La proprietà "currentPage" viene passata come prop allo componente "PageNav",
 quindi il componente può accedervi tramite la proprietà "props" definita 
 nello stesso componente 
*/
